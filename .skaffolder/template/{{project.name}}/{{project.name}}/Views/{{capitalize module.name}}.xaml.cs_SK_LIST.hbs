using {{project.name}}.ViewModels.ResourcesViewModel;
using System;
using Xamarin.Forms;
using Xamarin.Forms.Xaml;

namespace {{project.name}}.Views
{
    [XamlCompilation(XamlCompilationOptions.Compile)]
	public partial class {{capitalize crudResource.name}}List : ContentPage
	{
        //Set ViewModel for BindingContext
        private {{capitalize crudResource.name}}ListViewModel ViewModel
        {
            get
            {
                return BindingContext as {{capitalize crudResource.name}}ListViewModel;
            }
            set
            {
                BindingContext = value;
            }
        }
        
		public {{capitalize crudResource.name}}List ()
		{
            //Setting BindingContext
            ViewModel = new {{capitalize crudResource.name}}ListViewModel();
            InitializeComponent ();
		}

        protected override void OnAppearing()
        {
            base.OnAppearing();
            //Loading data with API request
            ViewModel.LoadDataCommand.Execute(null);           
        }

        private void SearchBar_TextChanged(object sender, TextChangedEventArgs e)
        {
            ViewModel.SearchCommand.Execute(null);
        }

        //Remove graphic effect on ListView
        private void MainListOf{{capitalize crudResource.name}}s_ItemSelected(object sender, SelectedItemChangedEventArgs e)
        {
            if (e.SelectedItem == null)
                return;
            ((ListView)sender).SelectedItem = null;
        }
    }
}