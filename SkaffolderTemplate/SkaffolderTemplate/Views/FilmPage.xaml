<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SkaffolderTemplate.Views.FilmPage"
             BackgroundColor="Gray"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <AbsoluteLayout>
            <Label Text="Lista di film" AbsoluteLayout.LayoutBounds="100,10" TextColor="Black" FontSize="32"/>
            <Button Text="Add" HorizontalOptions="StartAndExpand" AbsoluteLayout.LayoutBounds="80,50" WidthRequest="200" Clicked="aggiungiFilm"/>
            <StackLayout AbsoluteLayout.LayoutBounds="0, 100">
                <ListView x:Name="listaDiFilm" HorizontalOptions="Start" SeparatorColor="White" ItemTapped="cancellaElemento" IsPullToRefreshEnabled="True" Refreshing="OnRefresh">
                    <ListView.ItemTemplate>
                        <DataTemplate >
                            <TextCell Text="{Binding title}" Detail="{Binding genre}" TextColor="Black" DetailColor="Red"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>